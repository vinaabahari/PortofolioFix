<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kegiatan Bulan <%= month %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <style>
    /* === BASE === */
    body {
      background: linear-gradient(180deg, #fff3f7, #ffffff);
      font-family: 'Poppins', sans-serif;
      color: #333;
    }
    .container-custom {
      max-width: 1100px;
    }
    h2 {
      color: #ff6f91;
      font-weight: 700;
      letter-spacing: 0.5px;
      margin-bottom: 1rem;
      text-shadow: 0 2px 5px rgba(255, 111, 145, 0.2);
    }
    /* === CARD === */
    .card {
      border: none !important;
      border-radius: 25px;
      background: #fff;
      overflow: hidden;
      box-shadow: 0 8px 25px rgba(255, 111, 145, 0.25);
      transition: all 0.35s ease;
      position: relative;
    }
    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 12px 30px rgba(255, 111, 145, 0.4) !important;
    }
    .card img {
      width: 100%px;
      height: 250px;
      object-fit: cover;
      transition: all 0.3s ease;
    }
    .card:hover img {
      filter: brightness(1.05);
      transform: scale(1.03);
    }
    .card-body {
      padding: 20px 18px 25px;
    }
    .card-body h6 {
      color: #ff3b6b;
      font-size: 1.05rem;
      font-weight: 600;
      margin-bottom: 8px;
    }
    .card-body p {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 12px;
    }
    /* === BUTTON === */
    .btn-outline-pink {
      border: 2px solid #ff6f91;
      color: #ff6f91;
      border-radius: 25px;
      padding: 6px 18px;
      font-weight: 500;
      transition: all 0.3s ease;
      font-size: 0.9rem;
    }
    .btn-outline-pink:hover {
      background: linear-gradient(90deg, #ff8fa3, #ff6f91);
      color: #fff;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(255, 111, 145, 0.3);
    }
    /* === COLLAPSE === */
    .collapse p {
      font-size: 0.9rem;
      color: #555;
      background: #fff8fa;
      border-left: 3px solid #ff6f91;
      border-radius: 8px;
      padding: 10px 12px;
      margin-top: 8px;
    }
    /* === BACK BUTTON === */
    a[href*="projects"] {
      transition: all 0.3s ease;
    }
    a[href*="projects"]:hover {
      color: #ff3b6b;
      transform: scale(1.15);
    }
  </style>
</head>

<body>
  <%- include('layout/header') %>

  <a href="/#projects"
    style="position: fixed; top: 90px; left: 20px; font-size: 32px; color: #ff6f91; z-index: 2000; text-decoration: none;">
    <i class="bi bi-arrow-left-circle-fill"></i>
  </a>

  <div class="container py-5 container-custom">
    <h2 class="text-center mb-4">Kegiatan Bulan <%= month %></h2>

    <div class="row g-4 mt-2">
      <% if (!images || images.length === 0) { %>
        <p class="text-center text-muted">Belum ada foto di bulan ini.</p>
      <% } else { %>
        <% images.forEach(img => { %>
          <div class="col-lg-3 col-md-4 col-sm-6 col-12">
            <div class="card">
              <img src="<%= img.foto %>" class="card-img-top" alt="<%= img.judul %>">
              <div class="card-body text-left">
                <div class="d-flex justify-content-left align-items-left mb-2">
                  <h6 class="fw-bold mb-0 me-2"><%= img.judul %></h6>
                  <% if (img.link) { %>
                    <a href="<%= img.link %>" target="_blank" class="text-decoration-none link-icon">
                      <i class="bi bi-link-45deg"></i>
                    </a>
                  <% } %>
                </div>

                <% if (img.keterangan) { %>
                  <p class="text-muted mb-2"><%= img.keterangan %></p>
                <% } %>

                <% if (img.detail) { %>
                  <p>
                    <a class="btn btn-outline-pink btn-sm mt-2" data-bs-toggle="collapse"
                      href="#collapse<%= img.id %>" role="button" aria-expanded="false"
                      aria-controls="collapse<%= img.id %>">
                      Lebih lengkap
                    </a>
                  </p>

                  <div class="collapse text-start" id="collapse<%= img.id %>">
                    <p class="text-muted"><%= img.detail %></p>
                  </div>
                <% } %>
              </div>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
